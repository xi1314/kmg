package kmgHttpReverseProxy

/*
type RunServerRequest struct{
	SiteList []Site
}

type Site struct{
	Type SiteType  //网站配置模板名称,使用这个模板,然后再配置点东西就可以了.
	Domain string
	ListenAddr string //监听地址
	NextAddr string
	HttpsCert string // 证书内容
	HttpsKey string  // 证书私钥内容
}

type SiteType string
const (
	HttpSiteType SiteType = "Http" //http反向代理
	HttpsSiteType SiteType = "Https" //https反向代理
	HttpRedirectSiteType SiteType = "HttpRedirect" //http跳转
	HttpAndHttpsSiteType SiteType = "HttpAndHttps" //http和Https
)
*/
